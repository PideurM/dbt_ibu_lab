[project]
name = "dbt-biathlon"
version = "0.1.0"
description = "Project for dbt hands-on"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dbt-core>=1.11.3",
    "dbt-snowflake>=1.11.0",
    "snowflake-connector-python>=3.0.0",
]
